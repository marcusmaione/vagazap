<div class="form-box">
    <h5>Update job:</h5>
    <%= simple_form_for(@job, url: update_job_path(@company, @job), data: {turbo: false}, html: { method: :put }  ) do |f| %>
        <%= f.error_notification %>

        <%= f.input :title,
                    autofocus: true,
                    :label => "Title:",
                    collection: Job::JOBPOSITIONS %>
        <%= f.input :level,
                    :label => "Experience level:",
                    collection: Job::JOBLEVELS %>
        <%= f.input :sector,
                    :label => "Functional area:",
                    collection: Company::FUNCTIONALAREA %>
        <%= f.input :description,
                    :label => "Description:" %>
        <%= f.input :education_requirement,
                    :label => "Education requirement:",
                    collection: Course::DEGREELEVELS %>

        <div class="form-actions">
            <%= f.submit class: 'btn-primary', value: "Confirm" %>
        </div>
    <% end %>
    <br>
    <%= link_to "Back", :back, class: 'text-link' %>
</div>