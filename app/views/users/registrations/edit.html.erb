<div class="form-box">
    <h5>Update your profile:</h5>
    <%= simple_form_for(resource, as: resource_name, url: registration_path(resource_name), data: {turbo: false}, html: { method: :put }  ) do |f| %>
        <%= f.error_notification %>

        <%= f.input :first_name,
                    autofocus: true,
                    required: true,
                    :label => "First name:" %>
        <%= f.input :last_name,
                    required: true,
                    :label => "Last name:" %>
        <%= f.input :phone,
                    required: true,
                    :label => "Phone:" %>
        <%= f.input :city,
                    required: true,
                    :label => "Location:",
                    collection: User::CITIES %>
        <%= f.input :email,
                    required: true,
                    input_html: { autocomplete: "email" },
                    :label => "Email:" %>
        <%= f.input :password,
                    required: true,
                    hint: ("Leave blank if you don't want to change it; #{@minimum_password_length} minimum characters"),
                    input_html: { autocomplete: "new-password" },
                    :label => "Create a password:" %>
        <%= f.input :password_confirmation,
                    required: true,
                    input_html: { autocomplete: "new-password" },
                    :label => "Confirm password:" %>
        <%= f.input :current_password,
                    required: true,
                    hint: ("We need your current password to confirm your changes"),
                    input_html: { autocomplete: "current-password" },
                    :label => "Current password:" %>

        <div class="form-actions">
            <%= f.submit class: 'btn-primary', value: "Confirm" %>
        </div>
    <% end %>
    <br>
    <%= link_to "Back", :back, class: 'text-link' %>
</div>